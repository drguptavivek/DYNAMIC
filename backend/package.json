{
  "name": "dynamic-masters-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "vite --host 0.0.0.0 --port 8090 --strictPort",
    "build": "vite build",
    "test": "node --test"
  },
  "devDependencies": {
    "vite": "^8.0.16"
  }
}
