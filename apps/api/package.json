{
  "name": "@dynamic/api",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "test": "tsx --test \"src/**/*.test.ts\"",
    "test:integration": "npm run db:test:push && DATABASE_URL=${TEST_DATABASE_URL:-postgresql://dynamic:dynamic_dev_password@localhost:55432/dynamic_test} JWT_SECRET=test_jwt_secret JWT_REFRESH_SECRET=test_refresh_secret tsx --test \"src/**/*.integration.ts\"",
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit --project tsconfig.json",
    "start": "node dist/index.js",
    "replay:projections": "tsx src/dev/rebuild-projections.ts",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:test:ensure": "tsx src/dev/ensure-test-db.ts",
    "db:test:migrate": "npm run db:test:ensure && DATABASE_URL=${TEST_DATABASE_URL:-postgresql://dynamic:dynamic_dev_password@localhost:55432/dynamic_test} drizzle-kit migrate",
    "db:test:push": "npm run db:test:ensure && DATABASE_URL=${TEST_DATABASE_URL:-postgresql://dynamic:dynamic_dev_password@localhost:55432/dynamic_test} sh -c 'drizzle-kit push --dialect postgresql --schema \"./src/db/schema/*.ts\" --url \"$DATABASE_URL\"'",
    "db:push": "sh -c 'drizzle-kit push --dialect postgresql --schema \"./src/db/schema/*.ts\" --url \"$DATABASE_URL\"'",
    "smoke:dev": "tsx src/dev/smoke-dev.ts",
    "seed:large": "tsx src/dev/large-field-seed.ts"
  },
  "dependencies": {
    "@dynamic/event-core": "*",
    "@dynamic/shared-domain": "*",
    "@dynamic/shared-workflow": "*",
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.4.5",
    "drizzle-orm": "^0.45.2",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.1.1",
    "pg": "^8.11.5",
    "redis": "^4.7.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/express": "^4.17.21",
    "@types/jsonwebtoken": "^9.0.6",
    "@types/multer": "^2.1.0",
    "@types/node": "^20.0.0",
    "@types/pg": "^8.11.6",
    "drizzle-kit": "^0.22.7",
    "tsx": "^4.11.0",
    "typescript": "^5.4.5"
  }
}
